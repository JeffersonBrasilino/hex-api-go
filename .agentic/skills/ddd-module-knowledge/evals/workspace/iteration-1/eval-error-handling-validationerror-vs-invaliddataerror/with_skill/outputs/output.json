{ "id": 6, "answer": "ValidationError is used in the application or infrastructure layers when incoming request data fails basic structural validation (e.g. required fields missing), and maps to HTTP 400 Bad Request. InvalidDataError is used in the domain layer — in entities, value objects, and builders — when business invariants or domain-specific validation rules fail, and maps to HTTP 422 Unprocessable Entity." }