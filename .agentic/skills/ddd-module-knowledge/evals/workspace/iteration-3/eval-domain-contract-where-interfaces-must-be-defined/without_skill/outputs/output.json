{ "id": 14, "answer": "The repository interface for the product module should be defined in the domain layer, typically in the domain package (e.g., internal/product/domain/product.go or a dedicated contracts file). In hexagonal architecture, the domain layer owns the port (interface), and the infrastructure layer provides the adapter (concrete implementation). This keeps the domain independent of any persistence technology." }
