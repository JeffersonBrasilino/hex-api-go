{ "id": 10, "answer": "It depends on the complexity, but generally the Builder pattern is reserved for the aggregate root. Child entities are typically constructed directly via a constructor function (e.g., NewPerson(...)) rather than a full Builder. Using a Builder for every child entity adds unnecessary boilerplate. The Builder pattern is most valuable for complex aggregate roots with many optional fields and invariants to enforce during construction." }
