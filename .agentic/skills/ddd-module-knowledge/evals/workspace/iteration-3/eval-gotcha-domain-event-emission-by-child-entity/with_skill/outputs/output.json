{ "id": 9, "answer": "No. Only aggregate root entities emit domain events. Person is a child entity of the User aggregate root, so it must not emit domain events. Only User (the aggregate root) may emit them." }
