{ "id": 7, "answer": "No. The HTTP handler must not call the repository directly. In hexagonal architecture, the HTTP handler belongs to the infrastructure layer and should only interact with the application layer (command/query handlers or use cases). Direct access to the repository from the handler bypasses the domain and application layers, violating layer boundaries and the dependency rule." }
