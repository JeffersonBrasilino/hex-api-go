{ "id": 15, "answer": "No. The toDatabase and toDomain mapper functions should be unexported (lowercase: toDatabase, toDomain). They are internal implementation details of the database/infrastructure package and do not need to be visible outside that package. Keeping them unexported enforces encapsulation and prevents other layers from depending on persistence-specific mapping logic." }
